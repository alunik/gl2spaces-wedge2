import Wedge2Formalization.Paper.N4

/-!
Thin public row entry point for Appendix A, `n = 4`, row 3 (`S_1 + S_2`).

The row namespace is `Wedge2Formalization.Paper.N4.Row3`.
For a quick referee check, inspect:
* `paperRep₁`, `paperRep₂`
* `paperRep₁_transport`, `paperRep₂_transport`
* `mem_K_table_iff`
* `quotient_image`
-/
