import Wedge2Formalization.Paper.N5

/-!
Thin public row entry point for Appendix A, `n = 5`, row 1 (`S_3`).

The row namespace is `Wedge2Formalization.Paper.N5.Row1`.
For a quick referee check, inspect:
* `paperRep₁`, `paperRep₂`
* `paperRep₁_transport`, `paperRep₂_transport`
* `mem_K_table_iff`
* `quotient_image`
-/
