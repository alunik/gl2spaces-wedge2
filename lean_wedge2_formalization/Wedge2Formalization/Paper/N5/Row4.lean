import Wedge2Formalization.Paper.N5

/-!
Thin public row entry point for Appendix A, `n = 5`, row 4 (`S_1 + J_{a,2}`).

The row namespace is `Wedge2Formalization.Paper.N5.Row4`.
For a quick referee check, inspect:
* `paperRep₁`, `paperRep₂`
* `paperRep₁_transport`, `paperRep₂_transport`
* `mem_K_table_iff`
* `quotient_image`
-/
