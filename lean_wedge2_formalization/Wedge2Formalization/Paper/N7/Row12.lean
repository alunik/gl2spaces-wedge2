import Wedge2Formalization.Paper.N7

/-!
Thin public row entry point for Appendix A, `n = 7`, row 12 (`S_1 + J_{a,3}`).

The row namespace is `Wedge2Formalization.Paper.N7.Row12`.
For a quick referee check, inspect:
* `paperRep₁`, `paperRep₂`
* `paperRep₁_transport`, `paperRep₂_transport`
* `mem_K_table_iff`
* `quotient_image`
-/
