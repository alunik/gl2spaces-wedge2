import Wedge2Formalization.Paper.N7

/-!
Thin public row entry point for Appendix A, `n = 7`, row 16
(`S_1 + J_{a,1} + J_{b,1} + J_{c,1}`).

The row namespace is `Wedge2Formalization.Paper.N7.Row16`.
For a quick referee check, inspect:
* `paperRep₁`, `paperRep₂`
* `paperRep₁_transport`, `paperRep₂_transport`
* `mem_K_table_iff`
* `quotient_image`
-/
